<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="it.uniba.map.marketbasketanlysis">

    <application
        android:name="it.uniba.map.marketbasketanlysis.MarketBasketAnalysis"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.BasketMarketAnalysis">
        <activity
            android:name="it.uniba.map.marketbasketanlysis.ui.AnalysisActivity"
            android:exported="true"
            android:parentActivityName="it.uniba.map.marketbasketanlysis.ui.MainActivity">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="it.uniba.map.marketbasketanlysis.ui.MainActivity" />

        </activity>
        <activity
            android:name="it.uniba.map.marketbasketanlysis.ui.MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.BasketMarketAnalysis.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
    </application>

</manifest>